{
  "name": "alexis-kiara-pixel-story",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "generate:art": "node scripts/generate-art-assets.mjs",
    "repair:characters": "node scripts/repair-character-assets.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  }
}
